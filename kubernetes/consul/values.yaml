# Custom values for Consul
global:
  name: consul
server:
  replicas: 1  # Single replica for a local setup
ui:
  enabled: true
  service:
    type: NodePort
    port: 
      http: 8500
      https: null
    nodePort:
      http: 30850
      https: null
